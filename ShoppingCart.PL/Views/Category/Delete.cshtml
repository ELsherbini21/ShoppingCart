@model CategoryViewModel



<div class="form-body mt-3">
    <div class="row">
        <div class="form-holder">
            <div class="form-content">
                <div class="form-items">

                    <h3 class="mt-3 text-center">
                        <span class="badge bg-primary"> @Model.Name</span>
                    </h3>
                    <form asp-action="Delete" asp-route-id="@Model.Id" method="post" class="Create">
                        <input type="hidden" asp-for="Name" />
                        <input type="hidden" asp-for="DisplayOrder" />
                        <input type="hidden" asp-for="CreatedDateTime" />

                        <partial name="PartialViews/DetailsDeletePartial" model="@Model" />



                        <div class="form-button mt-3">
                            <a asp-action="Index" class="btn btn-info btn-lg w-25">Cancel</a>
                            <button id="submit" type="submit" class="btn btn-danger btn-lg w-25">Complete Delete </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section  scripts{
    <Partial name="_ValidationScriptsPartial" />
}