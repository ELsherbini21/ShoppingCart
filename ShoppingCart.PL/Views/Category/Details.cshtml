@model CategoryViewModel



<div class="form-body mt-3">
    <div class="row">
        <div class="form-holder">
            <div class="form-content">
                <div class="form-items">
                    <h3 class="mt-3 text-center">
                        <span class="badge bg-primary"> @Model.Name</span>
                    </h3>
                    <form asp-action="Create" method="post" class="Create">


                        <partial name="PartialViews/DetailsDeletePartial" model="@Model" />



                        <div class="form-button mt-3">
                            <a asp-action="Index" class="btn btn-info btn-lg w-25">Cancel</a>
                            <a asp-action="Update" asp-route-id="@Model.Id" class="btn btn-primary btn-lg w-25">Edit</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section  scripts{
    <Partial name="_ValidationScriptsPartial" />
}